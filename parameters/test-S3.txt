start='2018-07-26T00:00:00.000Z'
end='2018-07-27T00:00:00.000Z'

# Input data type
# For example 'olci' or 'msi'
sensor='olci'

# For using the HDA API, specify a region and one or multiple tiles from https://www.wekeo.eu/dataset-navigator/start
region='EU'
# example of multiple tiles: 'EU-042_012, EU-042_013'
tile='EU-042_012' 

# Specify the geometry for the subset
# Name of wkt file ('gva.wkt') available in perimeter folder, or string in format POLYGON(...)
wkt='gva.wkt'

# Specify the names of quicklook rgb/false color bands from L1 products
rgb_bands='Oa08_radiance, Oa06_radiance, Oa04_radiance'
false_color_bands='Oa17_radiance, Oa06_radiance, Oa03_radiance'

# Quicklook handling:
# - '1' (display)
# - '2' (save to disk)
qmode='2'

# Select processor combination (Ignored if qmode is '1' as everything is interactively selected)
# - '0' (Idepix) # Idepix is always applied, either pcombo includes '0' or not
# - '1' (C2RCC)
# - '2' (Polymer)
# - '3' (MPH)    # MPH is only available for OLCI
# example: '1, 2, 3' Will apply Idepix, C2RCC, Polymer and MPH
pcombo='1, 2, 3'

# Specify a valid expression for the mask (used only for c2rcc)
# OLCI example: ...
# MSI example: ...
validexpression='!pixel_classif_flags.IDEPIX_LAND and !pixel_classif_flags.IDEPIX_CLOUD and !quality_flags.invalid and quality_flags.fresh_inland_water'

# If qmode '2', and '1' in pcombo option is chosen, specify which c2rcc bands you want to plot
# Example: 'conc_chl, conc_tsm, iop_bwit'
c2rcc_bands='conc_chl, conc_tsm, iop_bwit'
# Define the maximum value to display for each c2rcc_bands (0 for automatic estimate)
c2rcc_maxbands='5,0,0'

# If qmode '2', and  '2' in pcombo option is chosen, specify which Polymer bands you want to plot
# Example: 'logchl, bbs, Rw665'
polymer_bands='logchl, Rw665'
# Define the maximum value to display for each polymer_bands (0 for automatic estimate)
polymer_maxbands='5,0'

# If qmode '2', and  '3' in pcombo option is chosen, specify which MPH bands you want to plot
# Example: 'chl, floating_cyanobacteria, floating_vegetation'
mph_bands='chl'
# Define the maximum value to display for each mph_bands (0 for automatic estimate)
mph_maxbands='5'

# Product handling (Ignored if pcombo = '0'):
# - '1' (L2 in memory/temp only)
# - '2' (save L2 to disk)
pmode='1'

# Select which API to use for the data query
# - COAH (Copernicus Open Access HUB; MSI and OLCI availalbe; MENTION DL LIMITATIONS...)
# - HDA (Wekeo Harmonized Data Access API; OLCI availalbe)
API='COAH'

# For COAH, you need to specify a user credentials for the Copernicus Open Access HUB
username='dani.odermatt'
password='ANSTALT133'